apiVersion: v1
kind: Service
metadata:
  name: frontend            # the Ingress in phase 30 will route to this
  namespace: polyshop
spec:
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - port: 80
      targetPort: 80    