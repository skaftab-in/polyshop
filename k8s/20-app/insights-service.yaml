apiVersion: v1
kind: Service
metadata:
  name: insights            # gateway calls http://insights:8000
  namespace: polyshop
spec:
  selector:
    app.kubernetes.io/name: insights
  ports:
    - port: 8000
      targetPort: 8000