apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: polyshop
spec:
  clusterIP: None           # headless again, same reasoning as postgres
  selector:
    app.kubernetes.io/name: redis
  ports:
    - port: 6379
      targetPort: 6379